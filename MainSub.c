#include <stdio.h>
/*
int main(void)
{
   int alpha[26];
   char x;
   char y;
   FILE *sub;
sub = fopen("sub.txt", "r");

for(int i=0; i<26; i++){
    
     fscanf(sub, "%c", &x);
     alpha[i] = x;
     //printf("%c", alpha[i]);
}


  FILE *message;
message = fopen("message.txt", "r");
   while(!feof(message)) {
       fscanf(message, "%c", &y);
       if(y==32){printf("%c",y);}else{
       for(int i=0; i<26; i++){
           if((alpha[i]==y)){
               
               printf("%c", i+65);
           }
           
           
       }
       }
       
       // commit not working
       
   }
}*/